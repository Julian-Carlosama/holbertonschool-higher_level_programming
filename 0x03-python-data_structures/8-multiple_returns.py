#!/usr/bin/python3
def multiple_returns(sentence):
    len_str = len(sentence)

    if len_str == 0:
        new_t = (len_str, none)
    else:
        new_t = (len_str, sentence[0])
    return(new_t)
